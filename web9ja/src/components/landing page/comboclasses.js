// ComboClasses.js
import classes from "./LandingPage.module.css"; // Import your styles

/* This is for more complex classes */

const ComboClasses = {
  /* All btns */

  prycta: `${classes.prycta} ${classes.black}`,
  seccta: `${classes.seccta} ${classes.accent1}`,
};

export default ComboClasses;
